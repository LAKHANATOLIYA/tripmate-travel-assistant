# TripMate environment example
# This project intentionally uses deterministic local tools and a mock weather dataset.
# Update these values only if you add a real LLM or weather provider later.

LLM_PROVIDER=deterministic_tool_router
WEATHER_DATA_SOURCE=mock
RAG_BACKEND=in_memory_similarity
